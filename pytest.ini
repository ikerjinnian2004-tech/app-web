[pytest]
pythonpath = .
markers =
    postgresql: pruebas destructivas contra una base dedicada cuyo nombre termina en _test
    docker_real: pruebas que crean contenedores reales y requieren RUN_DOCKER_SANDBOX_TESTS=1
    sandbox_adversarial: cargas agresivas solo para un host desechable y con opt-in explícito
